version: '3'

services:
  node:
    build:
      context: .
    ports:
      - "3035:3001"
    volumes:
      - ./:/app
      - node_modules:/app/node_modules

volumes:
  node_modules:

